# ============================================================
# requirements.txt — Dependências do DairyApp AI (LangGraph)
#
# Instalar com: pip install -r requirements.txt
#
# Organizado por categoria para facilitar entendimento.
# ============================================================

# --- LangChain / LangGraph (framework de agentes) ---
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# --- Semantic chunking (opcional, para strategy="semantic") ---
# Descomente se quiser usar semantic chunking:
# langchain-experimental>=0.3.4

# --- Reranking (opcional, para RERANKER=cohere) ---
# Descomente se quiser usar Cohere reranker:
# langchain-cohere>=0.3.0

# --- OpenAI (LLM + embeddings) ---
openai>=1.0.0

# --- Banco de dados ---
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# --- Servidor web ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# --- Utilitários ---
python-dotenv>=1.0.0
pydantic>=2.0.0

# --- Testes (opcional) ---
# pytest>=8.0.0
# httpx>=0.27.0
