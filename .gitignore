# Variáveis de ambiente (NUNCA commitar)
.env
.env.local
.env*.local

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
pytest-cache-files-*
*.egg-info/
dist/
build/
*.log

# Next.js / Node
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env
frontend/.env.local
frontend/.env*.local
frontend/tsconfig.tsbuildinfo
node_modules/

# OS
.DS_Store
Thumbs.db

# Claude Code (memória e configuração local do agente)
.claude/

# LangGraph runtime state (gerado automaticamente, não é código)
.langgraph_api/

# Ferramentas / binários locais
ngrok.exe
markitdown/

# PDFs da base de conhecimento (binários grandes — ingeridos via pipeline)
docs/agente-*/pdf/
docs/agente-*/pdf_ascii/
docs/backup/
